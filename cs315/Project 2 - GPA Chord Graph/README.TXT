Created by Brandon Jenkins

Everything seems like it works
	-Converted the entity from a vector to a BST
	-Getting similar values to the original chord graph, not sure if the jsons were the same
TO RUN
	g++ *.cpp
	./a.out cur_pp.json prev_pp.json
	-Results will be in cs315project2.json

Had to use both slip days for this project due to being sick last week.
